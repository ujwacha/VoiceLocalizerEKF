# Things TODO
- open another tcp socket, that is single threaded to recieve the input vector
- open another socket to send out the calculated state

Yeah it'd be better if we just used a message parser right??
